@extends('layouts.admin')

@section('content')
<div class="container-fluid">
    <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Manajemen Data Klinik</h1>
        <div class="btn-toolbar mb-2 mb-md-0">
            <a href="{{ route('klinik.create') }}" class="btn btn-sm btn-primary">
                <i class="fas fa-plus"></i> Tambah Klinik Baru
            </a>
        </div>
    </div>

    <!-- Add loading indicator -->
    <div id="loading-indicator" class="text-center py-4" style="display: none;">
        <div class="spinner-border text-primary" role="status">
            <span class="visually-hidden">Loading...</span>
        </div>
        <p class="mt-2">Memuat data klinik...</p>
    </div>

    @if(session('success'))
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        {{ session('success') }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    @endif

    @if(session('error'))
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        {{ session('error') }}
        <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
    </div>
    @endif

    <!-- Data Table Container -->
    <div id="data-container" class="card">
        <div class="card-header">
            <div class="d-flex justify-content-between align-items-center">
                <h5 class="card-title mb-0">
                    <i class="fas fa-clinic-medical"></i> Data Klinik Kecantikan
                    <span class="badge bg-primary ms-2">{{ $kliniks->total() ?? 0 }} Total</span>
                </h5>

                <!-- Search and Filter -->
                <div class="d-flex gap-2">
                    <form method="GET" class="d-flex">
                        <div class="input-group input-group-sm">
                            <input type="text"
                                   class="form-control"
                                   placeholder="Cari klinik..."
                                   name="search"
                                   value="{{ request('search') }}">
                            <select name="status" class="form-select">
                                <option value="">Semua Status</option>
                                <option value="approved" {{ request('status') == 'approved' ? 'selected' : '' }}>Disetujui</option>
                                <option value="pending" {{ request('status') == 'pending' ? 'selected' : '' }}>Pending</option>
                                <option value="rejected" {{ request('status') == 'rejected' ? 'selected' : '' }}>Ditolak</option>
                            </select>
                            <button class="btn btn-outline-primary" type="submit">
                                <i class="fas fa-search"></i>
                            </button>
                            @if(request('search') || request('status'))
                            <a href="{{ route('admin.kliniks.index') }}" class="btn btn-outline-secondary">
                                <i class="fas fa-times"></i>
                            </a>
                            @endif
                        </div>
                    </form>
                </div>
            </div>
        </div>

        @if(request('search') || request('status'))
        <div class="card-body border-bottom bg-light">
            <div class="row">
                <div class="col-md-6">
                    <small class="text-muted">
                        <i class="fas fa-filter"></i> Filter aktif:
                        @if(request('search'))
                        <span class="badge bg-info ms-1">Pencarian: "{{ request('search') }}"</span>
                        @endif
                        @if(request('status'))
                        <span class="badge bg-info ms-1">Status: {{ ucfirst(request('status')) }}</span>
                        @endif
                    </small>
                </div>
                <div class="col-md-6 text-end">
                    <small class="text-muted">
                        Menampilkan {{ $kliniks->count() }} dari {{ $kliniks->total() }} hasil
                    </small>
                </div>
            </div>
        </div>
        @endif
        <div class="card-body p-0">
            @if($kliniks && $kliniks->count() > 0)
            <div class="table-responsive">
                <table class="table table-striped table-hover align-middle mb-0">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col" class="text-center" style="width: 50px;">No</th>
                            <th scope="col" style="width: 70px;">Foto</th>
                            <th scope="col" style="min-width: 200px;">Nama Klinik</th>
                            <th scope="col" style="min-width: 220px;">Lokasi</th>
                            <th scope="col" style="min-width: 150px;">Rentang Harga</th>
                            <th scope="col" style="min-width: 200px;">Layanan</th>
                            <th scope="col" class="text-center" style="width: 100px;">Status</th>
                            <th scope="col" class="text-center" style="width: 120px;">Tanggal</th>
                            <th scope="col" class="text-center" style="width: 120px;">Aksi</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach($kliniks as $index => $klinik)
                        <tr>
                            <td class="text-center">
                                <small class="fw-bold text-primary">{{ $kliniks->firstItem() + $index }}</small>
                            </td>
                            <td>
                                @if($klinik->foto)
                                    <img src="{{ $klinik->foto_url }}"
                                         alt="Foto {{ $klinik->nama }}"
                                         class="img-thumbnail"
                                         style="width: 50px; height: 50px; object-fit: cover;">
                                @else
                                    <div class="bg-light border rounded d-flex align-items-center justify-content-center"
                                         style="width: 50px; height: 50px;">
                                        <i class="fas fa-clinic-medical text-muted fa-sm"></i>
                                    </div>
                                @endif
                            </td>
                            <td>
                                <div>
                                    <div class="fw-bold text-dark">{{ $klinik->nama }}</div>
                                    @if($klinik->telepon)
                                        <small class="text-muted d-block">
                                            <i class="fas fa-phone fa-xs"></i> {{ $klinik->telepon }}
                                        </small>
                                    @endif
                                    @if($klinik->email)
                                        <small class="text-muted d-block">
                                            <i class="fas fa-envelope fa-xs"></i> {{ Str::limit($klinik->email, 20) }}
                                        </small>
                                    @endif
                                </div>
                            </td>
                            <td>
                                <div>
                                    <div class="text-dark">{{ Str::limit($klinik->alamat, 35) }}</div>
                                    <small class="text-muted">
                                        <i class="fas fa-map-marker-alt fa-xs"></i>
                                        {{ number_format($klinik->latitude, 3) }}, {{ number_format($klinik->longitude, 3) }}
                                    </small>
                                </div>
                            </td>
                            <td>
                                @if($klinik->services && count($klinik->services) > 0)
                                    @php
                                        $services = collect($klinik->services);
                                        $totalServices = $services->count();
                                        $firstService = $services->first();
                                        $firstServicePrice = $klinik->service_prices[$firstService] ?? 0;
                                    @endphp

                                    <div class="service-price-info">
                                        <div class="service-name">
                                            {{ str_replace('_', ' ', Str::title($firstService)) }}
                                            @if($totalServices > 1)
                                                <span class="text-muted">
                                                    +{{ $totalServices - 1 }} layanan lainnya
                                                </span>
                                            @endif
                                        </div>
                                        <div class="service-categories">
                                            @php
                                                $categories = collect([
                                                    'facial_basic' => 'Perawatan Wajah',
                                                    'chemical_peel' => 'Peeling',
                                                    'laser_rejuvenation' => 'Laser',
                                                    'botox' => 'Injeksi & Estetik',
                                                    'microneedling' => 'Advanced',
                                                    'body_contouring' => 'Body'
                                                ]);

                                                $activeCategories = $categories->filter(function($label, $key) use ($services) {
                                                    return $services->contains($key);
                                                })->take(2);
                                            @endphp

                                            @foreach($activeCategories as $label)
                                                <span class="category-badge">{{ $label }}</span>
                                            @endforeach
                                        </div>
                                    </div>
                                @else
                                    <small class="text-muted">Belum ada layanan</small>
                                @endif
                            </td>
                            <td>
                                <div class="price-info">
                                    @if($klinik->min_price)
                                        <div class="current-price">
                                            Rp {{ number_format($klinik->min_price, 0, ',', '.') }}
                                        </div>
                                        @if($klinik->max_price && $klinik->max_price != $klinik->min_price)
                                            <div class="price-range text-muted">
                                                s/d Rp {{ number_format($klinik->max_price, 0, ',', '.') }}
                                            </div>
                                        @endif
                                    @else
                                        <small class="text-muted">Belum ada harga</small>
                                    @endif
                                </div>
                            </td>
                            <td class="text-center">
                                @if($klinik->status == 'pending')
                                    <div class="dropdown">
                                        <button type="button" class="btn btn-sm btn-warning dropdown-toggle"
                                                data-bs-toggle="dropdown" aria-expanded="false">
                                            <small>Pending</small>
                                        </button>
                                        <ul class="dropdown-menu dropdown-menu-end">
                                            <li>
                                                <form action="{{ route('admin.kliniks.update-status', $klinik) }}" method="POST">
                                                    @csrf
                                                    @method('PATCH')
                                                    <input type="hidden" name="status" value="approved">
                                                    <button type="submit" class="dropdown-item text-success">
                                                        <i class="fas fa-check fa-sm"></i> Setujui
                                                    </button>
                                                </form>
                                            </li>
                                            <li>
                                                <form action="{{ route('admin.kliniks.update-status', $klinik) }}" method="POST">
                                                    @csrf
                                                    @method('PATCH')
                                                    <input type="hidden" name="status" value="rejected">
                                                    <button type="submit" class="dropdown-item text-danger">
                                                        <i class="fas fa-times fa-sm"></i> Tolak
                                                    </button>
                                                </form>
                                            </li>
                                        </ul>
                                    </div>
                                @elseif($klinik->status == 'approved')
                                    <span class="badge bg-success">Disetujui</span>
                                @else
                                    <span class="badge bg-danger">Ditolak</span>
                                @endif
                            </td>
                            <td class="text-center">
                                <div>
                                    <small class="d-block fw-bold">{{ $klinik->created_at->format('d M Y') }}</small>
                                    <small class="text-muted">{{ $klinik->created_at->format('H:i') }}</small>
                                </div>
                            </td>
                            <td class="text-center">
                                <div class="btn-group btn-group-sm" role="group">
                                    <a href="{{ route('klinik.show', $klinik) }}"
                                       class="btn btn-outline-info btn-sm"
                                       title="Lihat Detail"
                                       target="_blank">
                                        <i class="fas fa-eye fa-xs"></i>
                                    </a>
                                    <a href="{{ route('admin.kliniks.edit', $klinik) }}"
                                       class="btn btn-outline-warning btn-sm"
                                       title="Edit">
                                        <i class="fas fa-edit fa-xs"></i>
                                    </a>
                                    <form action="{{ route('admin.kliniks.destroy', $klinik) }}"
                                          method="POST"
                                          class="d-inline delete-form">
                                        @csrf
                                        @method('DELETE')
                                        <button type="submit"
                                                class="btn btn-outline-danger btn-sm"
                                                title="Hapus">
                                            <i class="fas fa-trash fa-xs"></i>
                                        </button>
                                    </form>
                                </div>
                            </td>
                        </tr>
                        @endforeach
                    </tbody>
                </table>
            </div>

            <!-- Pagination -->
            @if($kliniks->hasPages())
            <div class="card-footer bg-light">
                <div class="row align-items-center">
                    <div class="col-md-6">
                        <small class="text-muted">
                            Menampilkan {{ $kliniks->firstItem() ?? 0 }} - {{ $kliniks->lastItem() ?? 0 }}
                            dari {{ $kliniks->total() }} klinik
                        </small>
                    </div>
                    <div class="col-md-6">
                        {{ $kliniks->appends(request()->query())->links('custom.pagination') }}
                    </div>
                </div>
            </div>
            @endif

            @else
            <!-- Empty State -->
            <div class="text-center py-5">
                <div class="mb-4">
                    <i class="fas fa-clinic-medical fa-4x text-muted mb-3"></i>
                    <h5 class="text-muted">Belum ada data klinik</h5>
                    <p class="text-muted mb-4">
                        @if(request('search') || request('status'))
                            Tidak ada klinik yang sesuai dengan filter pencarian Anda.
                            <br>
                            <a href="{{ route('admin.kliniks.index') }}" class="text-primary">
                                <i class="fas fa-arrow-left"></i> Kembali ke semua data
                            </a>
                        @else
                            Silakan tambah klinik baru untuk memulai mengelola data klinik kecantikan.
                        @endif
                    </p>
                    @if(!request('search') && !request('status'))
                    <a href="{{ route('klinik.create') }}" class="btn btn-primary">
                        <i class="fas fa-plus"></i> Tambah Klinik Pertama
                    </a>
                    @endif
                </div>
            </div>
            @endif
        </div>
    </div>
</div>
@endsection

@push('styles')
<style>
    /* Service badge styling */
    .badge.bg-primary {
        font-size: 0.7rem;
        font-weight: 500;
        padding: 0.35em 0.65em;
        margin: 0.1rem;
        background: linear-gradient(135deg, #0d6efd 0%, #0056b3 100%) !important;
        border: none;
        text-transform: capitalize;
    }

    /* Service info styling */
    .service-price-info {
        padding: 0.3rem 0;
    }

    .service-name {
        font-size: 0.9rem;
        color: #2c3e50;
        margin-bottom: 0.3rem;
        font-weight: 500;
    }

    .service-name .text-muted {
        font-size: 0.8rem;
        font-weight: normal;
    }

    .service-categories {
        display: flex;
        gap: 0.3rem;
        flex-wrap: wrap;
    }

    .category-badge {
        background: linear-gradient(135deg, #e3f2fd 0%, #bbdefb 100%);
        color: #1565c0;
        font-size: 0.75rem;
        padding: 0.2rem 0.5rem;
        border-radius: 4px;
        font-weight: 500;
    }

    /* Price info styling */
    .price-info {
        padding: 0.3rem 0;
    }

    .current-price {
        color: #198754;
        font-size: 0.95rem;
        font-weight: 600;
        margin-bottom: 0.2rem;
    }

    .price-range {
        font-size: 0.8rem;
    }

    /* Card styling */
    .card {
        border: none;
        box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
        border-radius: 0.5rem;
    }

    .card-header {
        background-color: #fff;
        border-bottom: 1px solid #e9ecef;
        padding: 1rem 1.25rem;
    }

    .card-title {
        color: #2c3e50;
        font-weight: 600;
        margin: 0;
    }

    /* Table styling */
    .table {
        margin-bottom: 0;
    }

    .table td {
        vertical-align: middle;
        padding: 1rem 0.75rem;
    }

    .table th {
        font-size: 0.875rem;
        font-weight: 600;
        text-transform: uppercase;
        letter-spacing: 0.5px;
        padding: 1rem 0.75rem;
        border-bottom: 2px solid #dee2e6;
        background-color: #f8f9fa;
        color: #495057;
    }

    .table-hover tbody tr:hover {
        background-color: rgba(0, 123, 255, 0.03);
    }

    /* Button & badge styling */
    .btn {
        font-weight: 500;
        padding: 0.375rem 0.75rem;
        border-radius: 0.375rem;
        transition: all 0.2s;
    }

    .btn-group-sm .btn {
        padding: 0.25rem 0.5rem;
        font-size: 0.75rem;
    }

    .btn-group-sm .btn i {
        font-size: 0.7rem;
    }

    .btn:hover {
        transform: translateY(-1px);
    }

    .badge {
        font-weight: 500;
        padding: 0.35em 0.65em;
        border-radius: 0.375rem;
    }

    /* Image styling */
    .img-thumbnail {
        border-radius: 0.375rem;
        border: 1px solid #dee2e6;
        padding: 0.25rem;
        background-color: #fff;
    }

    /* Filter & search styling */
    .input-group-sm > .form-control,
    .input-group-sm > .form-select,
    .input-group-sm > .btn {
        padding: 0.375rem 0.75rem;
        font-size: 0.875rem;
        border-radius: 0.375rem;
    }

    .form-control:focus,
    .form-select:focus {
        border-color: #86b7fe;
        box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
    }

    /* Pagination styling */
    .pagination {
        margin: 0;
    }

    .pagination-sm .page-link {
        padding: 0.375rem 0.75rem;
        font-size: 0.875rem;
        border-radius: 0.375rem;
    }

    .page-link:hover {
        z-index: 2;
        color: #0056b3;
        background-color: #e9ecef;
        border-color: #dee2e6;
    }

    .page-link:focus {
        z-index: 3;
        outline: 0;
        box-shadow: 0 0 0 0.2rem rgba(13, 110, 253, 0.25);
    }

    /* Status badges */
    .badge.bg-success {
        background-color: #198754 !important;
    }

    .badge.bg-warning {
        background-color: #ffc107 !important;
        color: #000;
    }

    .badge.bg-danger {
        background-color: #dc3545 !important;
    }

    /* Loading indicator */
    #loading-indicator {
        background-color: rgba(255, 255, 255, 0.8);
    }

    /* Responsive design */
    @media (max-width: 768px) {
        .table-responsive {
            font-size: 0.875rem;
        }

        .btn-group-sm .btn {
            padding: 0.2rem 0.4rem;
            font-size: 0.7rem;
        }

        .card-header {
            padding: 0.75rem 1rem;
        }

        .table td,
        .table th {
            padding: 0.75rem 0.5rem;
        }
    }

    /* Empty state */
    .text-center.py-5 {
        background-color: #f8f9fa;
        border-radius: 0.5rem;
    }

    .text-center.py-5 i {
        color: #6c757d;
        margin-bottom: 1rem;
    }

    /* Alert styling */
    .alert {
        border: none;
        border-radius: 0.5rem;
        padding: 1rem;
        margin-bottom: 1rem;
        font-size: 0.875rem;
    }

    .alert-dismissible .btn-close {
        padding: 1.25rem;
    }
</style>
@endpush

@push('scripts')
<script>
$(document).ready(function() {
    // Hide loading indicator when page loads
    $('#loading-indicator').hide();
    $('#data-container').fadeIn();

    // Delete confirmation
    $('.delete-form').on('submit', function(e) {
        e.preventDefault();
        const form = this;

        Swal.fire({
            title: 'Apakah Anda yakin?',
            text: "Data klinik akan dihapus permanen dan tidak bisa dikembalikan!",
            icon: 'warning',
            showCancelButton: true,
            confirmButtonColor: '#dc3545',
            cancelButtonColor: '#6c757d',
            confirmButtonText: 'Ya, hapus!',
            cancelButtonText: 'Batal',
            reverseButtons: true
        }).then((result) => {
            if (result.isConfirmed) {
                // Show loading
                Swal.fire({
                    title: 'Menghapus...',
                    text: 'Sedang memproses penghapusan data',
                    allowOutsideClick: false,
                    didOpen: () => {
                        Swal.showLoading();
                    }
                });
                form.submit();
            }
        });
    });

    // Status update confirmation
    $('form[action*="status"]').on('submit', function(e) {
        e.preventDefault();
        const form = this;
        const status = $(this).find('input[name="status"]').val();
        const statusText = status === 'approved' ? 'disetujui' : 'ditolak';

        Swal.fire({
            title: `Konfirmasi Status`,
            text: `Apakah Anda yakin ingin mengubah status menjadi "${statusText}"?`,
            icon: 'question',
            showCancelButton: true,
            confirmButtonColor: status === 'approved' ? '#28a745' : '#dc3545',
            cancelButtonColor: '#6c757d',
            confirmButtonText: 'Ya, ubah status',
            cancelButtonText: 'Batal'
        }).then((result) => {
            if (result.isConfirmed) {
                form.submit();
            }
        });
    });

    // Auto-hide alerts after 5 seconds
    setTimeout(function() {
        $('.alert').fadeOut('slow');
    }, 5000);
});
</script>
@endpush
